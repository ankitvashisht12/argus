# Progress

## Done
- Design spec + contract (docs/superpowers/specs/, docs/loop/)
- Engine: diff parser, claude adapter, gh client, review pipeline, cache — 95/95 tests, adversarial-verified (items 1-9)
- Extension: tree, diff provider, AI comment threads, overview, sidebar chat, GitHub review submit — 39/39 pure tests, adversarial-verified (items 10-20), evaluator findings fixed
- Release: root README, THIRD-PARTY-NOTICES, live smoke test 12/12 (sindresorhus/is#210, real claude call, anchors verified), final release sweep releaseReady=true, low findings fixed (incl. NUL-byte cleanup in sidebar.js)
- argus-review-0.1.0.vsix packaged (13 files, 47.4 KB) and installed locally
- Contract: 24/24 checked
- v0.1.1 (live-testing fixes): per-hunk coverage (prompt contract + buildReviewSchema(N) minItems + tolerant alias resolution), start-line single-line anchoring (firstChangedLine + aiThreadRange), split File Details webview (argus.details) so activity bar shows Changed Files + File Details accordions, chat relocated to argus-chat panel container with one-time hint, submit-review UX (status-bar count + commentThread/title action) — engine 107 / vscode 51 tests, all gates green; argus-review-0.1.1.vsix installed; tagged v0.1.1
- v0.1.2 (progressive loading): PrSession.load is now two-stage — resolves as soon as meta+diff+parsed files+persisted UI state are ready (tree/diffs/details usable immediately), then starts the AI review in the background via new startReview() (respects the cache; cache hit still near-instant). New reviewStatus getter ('idle'|'running'|'ready'|'error') + reviewSettled() promise; onDidChangeReview fires on 'running' too. extension.ts: fetch-only progress notification, new "$(sync~spin) ARGUS reviewing…" status-bar item (priority 101, beside submit item at 100) shown while running, errored-review warning now event-driven via onDidChangeReview. Regenerate reuses the background path (non-blocking). Overview/details already key off review-null-no-error → show loading; comments rebuild on review-land (verified). Demo sessions still ready at load. +4 vscode tests (55 total) — engine 107 / vscode 55, all gates green; argus-review-0.1.2.vsix installed; tagged v0.1.2
- v0.1.3 (three fixes, evaluator-verified): (A) **session-less diff placeholder** — a restored `argus://` diff tab (window reload, or a different PR loaded) no longer renders the scary "load error" document; new pure `placeholderBanner(parts)` in contentProvider shows a calm, URI-derived message naming the PR (#N of owner/repo) and the "ARGUS: Review PR…" command. Guarded by `!session || !sessionMatchesUri(session, parts)` so a tab for PR #5 while #9 is loaded self-heals instead of fetching #9's SHAs. (B) **size-scaled review timeout** — the fixed 90s default timed out real PRs now that the prompt requires a note per hunk. New engine `computeReviewTimeoutMs`/`resolveReviewTimeoutMs` (base 120s + 15s/hunk + 1s/KB, capped 600s; positive `argus.reviewTimeoutSeconds` setting overrides verbatim, uncapped), read fresh per review and threaded through `#runReview`; timeout failures get an actionable message. (C) **AI reading-order file buckets** — engine `heuristicBucket`/`bucketFiles`/`READING_BUCKETS` group changed files into reading-ordered buckets (heuristic fallback before the review lands, model `bucket`/`readingOrder` guidance after); schema/prompt now require per-file reading guidance; tree.ts renders collapsible `argusBucket` group nodes with flat file rows in reading order (parent dir shown as row description). Reviewed-toggle still targets `argusFile.*` only. engine 170 / vscode 67 tests, all gates green; argus-review-0.1.3.vsix installed; tagged v0.1.3

- v0.1.4 (three improvements): (1) **persistent draft comments** — draft-thread registry now serialized via the session KeyValueStore (new `DRAFTS_KEY`/`PersistedDraft`/`PrSession.drafts`/`saveDrafts`); comments.ts persists on every add/edit/delete + post-submit clear, and `syncSession` restores same-PR drafts on reload (recreating threads on the correct argus:// URIs via `buildArgusUri`) while different-PR still discards-with-toast (now also wipes the previous PR's persisted copy so a discard is final). (2) **uncovered-hunks indicator** — engine's `uncoveredHunkIds` surfaced on `OverviewModel.uncoveredCount`; Overview tab shows a calm "N hunks not covered — Regenerate to retry" line + button, plus a once-per-review warning toast in extension.ts (`notifyUncoveredHunks`). (3) **self-healing restored tabs** — new `notifyContentProviderSessionChanged()` fires `onDidChange` for every open argus:// doc matching the adopted PR (was only fired lazily from `provideTextDocumentContent`), called from `refreshSurfaces`. engine 170 / vscode 76 tests (+9), all gates green; argus-review-0.1.4.vsix installed; tagged v0.1.4

## Doing
- (nothing — v0.1.4 shipped)

## Next
- Screenshots for README (needs a human eye)
- Marketplace publish (needs Ankit's publisher account + intent)
- Fast-follows: local branch mode, GitHub comment-thread sync, web/tui faces
