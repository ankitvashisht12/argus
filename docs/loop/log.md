# Log

## [2026-07-14] session start | spec + contract written; toolchain verified (gh authed, claude 2.1.209, node 22.14); usage watcher running
## [2026-07-14] engine integrated | 5 modules unified; full index.ts barrel; retired stale stub-throws smoke assertion; 95/95 vitest green, tsc clean, no vscode imports; root npm install + workspace scripts verified
## [2026-07-14] extension built | extension.ts wired to all six surfaces; unified argus:// URI shape across contentProvider/comments/sidebar/tree; gh/claude-missing actionable notifications, diff opens without claude (contract 18); added flat eslint (engine+vscode, clean), 31 vscode pure-logic vitest + 95 engine green, tsc clean, esbuild bundle clean; packaged argus-review-0.1.0.vsix (47 KB, vsce --no-dependencies)
## [2026-07-14] adversarial fixes r1 | fixed 3 defects: clear stale drafts on session swap (comments.ts), demo regenerate no-op re-emits fixture (prSession.ts), renamed-file old-side AI thread URIs via shared argusUriForSide helper (comments.ts+contentProvider.ts); +8 vscode tests (39 total) + 95 engine green; tsc/build/eslint/package all clean
## [2026-07-14] release | 4 low findings fixed (stale TSDoc x3, vsce footgun README note, renderMarkdown guard + NUL-byte escape cleanup); all gates green; vsix repackaged + reinstalled; contract 24/24; tagged v0.1.0
## [2026-07-14] v0.1.1 fixes | from live PR user testing: (A) per-hunk coverage — prompt now demands one entry per alias + buildReviewSchema(N) minItems wired via prSession, alias resolution case/whitespace-tolerant; (B/C) start-line anchoring — firstChangedLine() anchors each note to the hunk's first changed line as a single-line range (was whole-hunk span rendering at hunk END), verified fixture @@ -10,6 +12,8 @@ +3 ctx → new line 15 / 0-based 14; split File Details into own argus.details webview (Changed Files + File Details accordions in activity bar), chat moved to argus-chat panel container + one-time relocation hint; submit-review UX — status-bar "Submit Review to GitHub (N)" + commentThread/title action bound to argus.submitReview. Gates: engine 107, vscode 51, tsc/eslint/esbuild/vsce clean; argus-review-0.1.1.vsix installed; tagged v0.1.1
## [2026-07-14] v0.1.2 progressive loading | live-testing feedback: whole UI blocked until gh fetch AND AI review both finished. Root cause: PrSession.load awaited #runReview (prSession.ts:303) before resolving, and extension.ts adopted the session only after (extension.ts:188). Fix — two-stage load: stage 1 (awaited) fetch meta+diff+parse+kv state → session returned with tree/diffs/details live; stage 2 (fire-and-forget via new startReview()) runs review in background, cache-respecting. New reviewStatus 'idle'|'running'|'ready'|'error' + reviewSettled() promise + #reviewInFlight; startReview fires onDidChangeReview on 'running', #setReview→'ready', #setReviewError→'error' (contract 19 held: error ⇒ review null). extension.ts: progress notification now fetch-only ("fetching…"), new "$(sync~spin) ARGUS reviewing…" status-bar item (prio 101) shown while running, errored-review toast moved to onDidChangeReview handler (onReviewChanged), regenerate non-blocking (void session.regenerate()). Overview/details loading state already keyed off review-null-no-error; comments rebuild on land — verified. +1 test file prSession.status.test.ts (4 tests: running→ready, claude-unavailable→error, regenerate running→ready, demo ready-at-load) via stubbed gh/agent + tmp storage. Gates: engine 107, vscode 55, tsc/eslint/esbuild/vsce clean; argus-review-0.1.2.vsix installed; tagged v0.1.2
