# Log

## [2026-07-14] session start | spec + contract written; toolchain verified (gh authed, claude 2.1.209, node 22.14); usage watcher running
## [2026-07-14] engine integrated | 5 modules unified; full index.ts barrel; retired stale stub-throws smoke assertion; 95/95 vitest green, tsc clean, no vscode imports; root npm install + workspace scripts verified
## [2026-07-14] extension built | extension.ts wired to all six surfaces; unified argus:// URI shape across contentProvider/comments/sidebar/tree; gh/claude-missing actionable notifications, diff opens without claude (contract 18); added flat eslint (engine+vscode, clean), 31 vscode pure-logic vitest + 95 engine green, tsc clean, esbuild bundle clean; packaged argus-review-0.1.0.vsix (47 KB, vsce --no-dependencies)
## [2026-07-14] adversarial fixes r1 | fixed 3 defects: clear stale drafts on session swap (comments.ts), demo regenerate no-op re-emits fixture (prSession.ts), renamed-file old-side AI thread URIs via shared argusUriForSide helper (comments.ts+contentProvider.ts); +8 vscode tests (39 total) + 95 engine green; tsc/build/eslint/package all clean
## [2026-07-14] release | 4 low findings fixed (stale TSDoc x3, vsce footgun README note, renderMarkdown guard + NUL-byte escape cleanup); all gates green; vsix repackaged + reinstalled; contract 24/24; tagged v0.1.0
## [2026-07-14] v0.1.1 fixes | from live PR user testing: (A) per-hunk coverage — prompt now demands one entry per alias + buildReviewSchema(N) minItems wired via prSession, alias resolution case/whitespace-tolerant; (B/C) start-line anchoring — firstChangedLine() anchors each note to the hunk's first changed line as a single-line range (was whole-hunk span rendering at hunk END), verified fixture @@ -10,6 +12,8 @@ +3 ctx → new line 15 / 0-based 14; split File Details into own argus.details webview (Changed Files + File Details accordions in activity bar), chat moved to argus-chat panel container + one-time relocation hint; submit-review UX — status-bar "Submit Review to GitHub (N)" + commentThread/title action bound to argus.submitReview. Gates: engine 107, vscode 51, tsc/eslint/esbuild/vsce clean; argus-review-0.1.1.vsix installed; tagged v0.1.1
## [2026-07-14] v0.1.2 progressive loading | live-testing feedback: whole UI blocked until gh fetch AND AI review both finished. Root cause: PrSession.load awaited #runReview (prSession.ts:303) before resolving, and extension.ts adopted the session only after (extension.ts:188). Fix — two-stage load: stage 1 (awaited) fetch meta+diff+parse+kv state → session returned with tree/diffs/details live; stage 2 (fire-and-forget via new startReview()) runs review in background, cache-respecting. New reviewStatus 'idle'|'running'|'ready'|'error' + reviewSettled() promise + #reviewInFlight; startReview fires onDidChangeReview on 'running', #setReview→'ready', #setReviewError→'error' (contract 19 held: error ⇒ review null). extension.ts: progress notification now fetch-only ("fetching…"), new "$(sync~spin) ARGUS reviewing…" status-bar item (prio 101) shown while running, errored-review toast moved to onDidChangeReview handler (onReviewChanged), regenerate non-blocking (void session.regenerate()). Overview/details loading state already keyed off review-null-no-error; comments rebuild on land — verified. +1 test file prSession.status.test.ts (4 tests: running→ready, claude-unavailable→error, regenerate running→ready, demo ready-at-load) via stubbed gh/agent + tmp storage. Gates: engine 107, vscode 55, tsc/eslint/esbuild/vsce clean; argus-review-0.1.2.vsix installed; tagged v0.1.2
## [2026-07-14] v0.1.4 | three improvements shipped together. (1) persistent draft comments: draft-thread registry was in-memory only, so a window reload lost the reviewer's drafts (breaking the landing page's stop&resume promise). Fix — new engine-store-backed persistence mirroring the REVIEWED_KEY/CHAT_KEY pattern: prSession.ts gets DRAFTS_KEY, PersistedDraft {path,line,side,body,id?}, load reads drafts from kv, new `drafts` getter + `saveDrafts()` (updates in-memory snapshot + writes kv; demo/#kv=null no-ops). comments.ts persists the live registry (getDraftComments()) on every createDraft/saveDraft/deleteDraft via persistDrafts(), and clearDrafts() now also writes [] to the bound session (post-submit + different-PR discard both make the wipe final). syncSession rewritten: compares PR identity (sameProject owner/repo/number) BEFORE rebinding so discard targets the previous session's kv; discards+toast only on different-PR (or null) swap; restores persisted drafts (restoreDrafts recreates threads on argus:// URIs rebuilt with buildArgusUri — exact inverse of parseDiffUri, so renamed-file base-side drafts land on the oldPath doc) when the live registry is empty (fresh module load after reload, or right after a different-PR discard). Same-PR reload therefore resumes; different-PR discards. (2) uncovered-hunks indicator: engine normalizeReview already returned uncoveredHunkIds but it was never surfaced. OverviewModel gains uncoveredCount (= session.review.uncoveredHunkIds.length in ready state, 0 otherwise); overview.js renders a warn-tinted "N hunk(s) not covered by this review." line + "Regenerate to retry" button (posts {type:'regenerate'}); overview.css .uncovered-line/.btn-small. extension.ts onReviewChanged fires notifyUncoveredHunks on reviewStatus==='ready' — a once-per-settled-review warning toast + Regenerate action + output-channel log (onDidChangeReview emits 'ready' once per run; Regenerate = fresh run = legit fresh toast). Calm by design — schema minItems forces full coverage, so this is the honesty backstop. (3) self-healing restored tabs (v0.1.3 evaluator LOW): a restored argus:// diff tab kept showing placeholderBanner even after the matching PR loaded, because contentProvider.syncSession only ran (and fired onDidChange) from inside provideTextDocumentContent — a tab VS Code never re-requested stayed stuck. Fix: new module-level notifyContentProviderSessionChanged() → refreshOpenDocuments() invalidates cache + iterates vscode.workspace.textDocuments, firing onDidChange for every open argus:// doc whose URI sessionMatchesUri the adopted PR; wired into extension.ts refreshSurfaces(). Tests: +9 (prSession.drafts.test.ts x3 restore/clear/demo, overviewModel.test.ts x3 uncoveredCount ready/covered/empty, uri.test.ts sessionMatchesUri x3). Gates: engine 170, vscode 76, tsc/eslint/esbuild clean, vsce packaged argus-review-0.1.4.vsix (16 files, 71.24 KB) installed; tagged v0.1.4
## [2026-07-14] v0.1.3 | three fixes shipped together, evaluator-verified. (A) session-less diff placeholder: restored argus:// diff tabs (window reload / different PR loaded) showed errorBanner "This is a load error" via the !session branch of provideTextDocumentContent — VS Code re-opens custom-scheme diff editors on reload while extension activates with currentSession=null. Fix: new pure placeholderBanner(parts) derives a calm message from the URI (PR #N of owner/repo + "ARGUS: Review PR…" command); shown on !session OR !sessionMatchesUri (identity guard also fixes latent wrong-PR-SHA fetch when tab#5 restored while PR#9 loaded). Self-heals via existing syncSession cache-invalidation on session change. (B) size-scaled review timeout: prSession called runStructured with no timeoutMs → fixed 90s DEFAULT_TIMEOUT_MS, which times out real PRs now the prompt requires one note per hunk. New engine computeReviewTimeoutMs (base 120s + 15s/hunk + 1s/KB, clamp 600s) + resolveReviewTimeoutMs (positive argus.reviewTimeoutSeconds setting wins verbatim/uncapped, else computed); read fresh per review via injectable thunk, threaded as timeoutMs; #reviewErrorMessage rewrites /timed out/ into actionable text. Verified math: 2 hunks→~150s, 30→570s, 200→600s(cap), override 420→420_000. Chat timeout untouched (still 90s). (C) AI reading-order buckets: engine READING_BUCKETS + heuristicBucket (most-specific-first: lockfiles/dist→Generated, .github/*.yml→Config&CI, *.test.ts→Tests) + bucketFiles (heuristic fallback pre-review, model bucket/readingOrder post-review, defensive vs bogus paths/blank labels); schema requires bucket+readingOrder, prompt instructs foundational→dependents→tests→docs→generated LAST; tree.ts renders argusBucket collapsible group nodes with flat reading-ordered file rows (parentDir as description, no directory nesting), reviewed-toggle menu still viewItem=~/argusFile/. Cross-fixer collision check: index.ts merged both export blocks cleanly, types.ts only touched by (C), no dup exports (typecheck clean). Gates: engine 170, vscode 67, tsc/eslint/esbuild clean, vsce packaged argus-review-0.1.3.vsix (16 files, 70.1 KB) installed; tagged v0.1.3
